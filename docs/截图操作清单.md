# 截图操作清单（报告 5.4 节 图 5-1 ~ 图 5-16）

> 用途：按本清单逐图截取系统运行效果截图，回填《课程设计报告》5.4 节（图号+图注已列于报告表）。
> 保存位置：全部存到 `docs/screenshots/`，文件名按清单第三列（如 `docs/screenshots/图5-1.png`）。
> 截图要求：窗口最大化、包含足够上下文（能看到页面标题/命令与输出）、不要出现与本设计无关的界面元素。
> 当前系统状态（2026-09-13）：全部容器健康运行；统计数据已恢复；证据图中文正常；复核闭环在跑（每几分钟有自动复核任务）。

| 图号 | 图注（报告 5.4 表） | 建议文件名 | 操作步骤 | 预期画面内容 |
|---|---|---|---|---|
| 图 5-1 | 容器编排启动状态 | 图5-1.png | 在 `docker/` 目录执行 `docker compose ps`，截终端输出 | 12 个容器（六组件 + backend-1/2 + 4 个仿真）全部 Up/healthy |
| 图 5-2 | HDFS Web UI | 图5-2.png | 浏览器打开 `http://localhost:9870/explorer.html`（或 9870 首页 Overview） | NameNode 概览：容量/块数/副本数（Live Nodes 1，复制因子 2） |
| 图 5-3 | HDFS 巡检影像目录 | 图5-3.png | 浏览器 HDFS Utilities → Browse the file system，逐级进入 `/inspection/alarm_snapshot/2026/09/13/` 任一设备目录 | 证据图 PNG 文件列表，路径符合 `/inspection/{type}/{yyyy}/{MM}/{dd}/{deviceId}/{uuid}.png` |
| 图 5-4 | Kafka 主题列表与分区 | 图5-4.png | 执行 `docker exec kafka /opt/kafka/bin/kafka-topics.sh --bootstrap-server localhost:9092 --describe` 截输出 | 8 个主题及分区数/ISR（uav.telemetry 3 分区、inspection.alarm 3 分区等） |
| 图 5-5 | Kafka 消息生产消费 | 图5-5.png | 执行 `docker exec kafka /opt/kafka/bin/kafka-console-consumer.sh --bootstrap-server localhost:9092 --topic uav.telemetry --max-messages 6` 截输出 | 连续遥测消息，同设备消息有序（key=deviceId 同分区） |
| 图 5-6 | MongoDB 集合与文档 | 图5-6.png | 执行 `docker exec mongodb mongosh --quiet --eval 'db.getSiblingDB("inspection").alarm.findOne({review:{$exists:true}})'` 截输出 | 告警文档含 `snapshotPath`（高空原图）与 `review` 复核子文档（结论/复核图路径/复核设备）证据链 |
| 图 5-7 | ES 索引 mapping | 图5-7.png | 浏览器打开 `http://localhost:8080/kibana/` → 左侧 Dev Tools → `GET /inspection_alarm_v3/_mapping` 执行，截结果（或 curl 输出） | mapping 含 `dynamic:strict`、`location: geo_point`、`description: ik_smart`、keyword 字段 |
| 图 5-8 | ES 组合检索结果 | 图5-8.png | 浏览器前端「告警检索」页：类型=周界入侵 + 时间=now-24h~now，点检索，截结果列表 | 命中告警列表（Mongo 权威 + ES 检索副本语义） |
| 图 5-9 | Kibana 仪表盘 | 图5-9.png | 浏览器 Kibana → Discover：Data view 选 `inspection_alarm_v3`，时间选近 24 小时，截查询结果 | 告警数据在 Kibana 可检索（本环境未预建仪表盘，以 Discover 视图呈现，如实标注） |
| 图 5-10 | 前端设备地图总览 | 图5-10.png | 浏览器 `http://localhost:8080/` 总览页，等待 10 秒让地图刷新 | 无人机（蓝圆）与机器狗（绿圆）图标+编号标签分布园区内，告警点叠加 |
| 图 5-11 | 前端告警中心与详情 | 图5-11.png | 告警页选一条状态=确认属实（CONFIRMED）的告警，点「详情」，截详情面板 | 左侧「高空发现原图/事件记录图」+右侧「机器狗红外复核图」+复核结论与设备并列展示 |
| 图 5-12 | 前端任务下发与跟踪 | 图5-12.png | 任务页：新建一个 POINT_REVIEW 任务（选在线机器狗+目标点）→ 观察任务行状态流转（DISPATCHED→RUNNING→DONE），截任务列表 | 任务状态流转与备注/目标坐标可见 |
| 图 5-13 | 前端统计看板 | 图5-13.png | 浏览器「统计」页，截整页 | 在线率、任务完成率、告警类型分布与 24h 趋势图 |
| 图 5-14 | Nginx 负载均衡效果 | 图5-14.png | 执行 `1..10 | %{ curl -s -D- -o /dev/null http://localhost:8080/api/devices | grep -i x-backend-instance }`（或浏览器 F12 刷新 10 次看响应头），截输出 | 多行 `X-Backend-Instance: backend-1/backend-2` 交替出现 |
| 图 5-15 | 停实例后的故障转移 | 图5-15.png | 依次执行并截图：① `docker compose stop backend-1`；② `curl -s -D- -o /dev/null http://localhost:8080/api/devices | grep -i x-backend-instance`；③ `docker compose start backend-1` | 停 backend-1 后请求仍 200 且由 backend-2 服务 |
| 图 5-16 | 设备离线检测 | 图5-16.png | 前端设备页：任选一台设备点「下线」→ 约 5 秒后刷新看状态变 OFFLINE；再到告警页看新产生 DEVICE_OFFLINE 告警；截两张或并排 | 设备 OFFLINE 徽标 + DEVICE_OFFLINE 告警（手动下线 5s 生效，S50 提速） |

**截完通知我**：我会把图片插入报告 5.4 表对应行、核对图注并升版修订记录（S65 回填）。
