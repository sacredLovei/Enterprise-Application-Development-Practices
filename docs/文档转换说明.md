# 文档转换说明（S73）

**已产出**（工作区根目录）：
- `课程设计报告_第1-7章_园区安防巡检平台.html`（205 KB，浏览器可直接查看）
- `课程设计报告_第1-7章_园区安防巡检平台.docx`（207 KB，3573 段 / 43 表，Word 打开正常）

**工具链**：`docs/工具/convert.js`（marked 转换器，GFM 表格）。重新生成步骤：

```powershell
cd tools\md2html
npm install marked --registry https://registry.npmmirror.com
node convert.js "..\..\课程设计报告_第2-6章_园区安防巡检平台.md" "..\..\课程设计报告_第1-7章_园区安防巡检平台.html"
# 再手工用 Word 打开该 HTML 另存为 .docx（或按脚本用 Word COM 自动转）
```

**剩余需小组完成的事项**（AI 无法代办，依赖课程模板与人工信息）：
1. **套用课程模板样式**——将 docx 内容粘贴进《企业应用开发实践课程设计报告.docx》模板（或按模板要求调整标题/正文字体字号行距）；
2. **封面**：学院/专业/班级/姓名/学号/指导教师（模板字段）；
3. **小组分工表**：成员与任务分解（模板要求）；
4. **目录**：Word 引用 → 目录（自动生成，基于标题样式）；
5. **截图插入**：S65 的 16 张截图（`docs/screenshots/`）插入 5.4 节后，重新走一遍转换；
6. **最终校对**：页眉页脚、页码、图表编号连续性。

> 注意：截图完成前 docx 为"无图版"；报告 5.4 节表格已就位，插入图片后把状态列从 `【待回填】` 改为对应图片路径即可。
